export default class ClassRoom { // Prototype class for ClassRoom
// Constructor function for ClassRoom class with parameter maxStudentsSize (number)
  constructor(maxStudentsSize) {
    this._maxStudentsSize = maxStudentsSize; // Private variable for maxStudentsSize
  }
}
